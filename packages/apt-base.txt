# packages/apt-base.txt — headless Linux baseline, installed by
# scripts/bootstrap-apt.sh on Debian/Ubuntu hosts (always, no gate).
#
# Scope: only what this repo's own machinery needs when the Brewfile is
# skipped (install.sh skips it on non-macOS unless GUI_INSTALL=1), plus the
# documented Homebrew-on-Linux prerequisites. The Brewfile remains the source
# of truth for the full toolset; add day-to-day tools there, not here.
#
# Format: one apt package per line; '#' starts a comment (inline allowed).

# repo machinery
zsh        # install.sh + scripts/sync-coding-tools.sh shebangs, Makefile shell
rsync      # copy-mode sync in scripts/sync-coding-tools.sh
make       # Makefile entrypoints (make install / make sync / ...)
unzip      # archive extraction assumed by various installers

# Homebrew on Linux prerequisites
# https://docs.brew.sh/Homebrew-on-Linux#requirements
build-essential
ca-certificates
curl
file
git
procps
