# address-worklist-commit-loop swarm leftovers

8 variants of `.cursor/commands/address-worklist-commit-loop.md` from an earlier
swarm experiment, archived before the canonical version was selected and merged
into main at `.cursor/commands/address-worklist-commit-loop.md`. Each `cand-N.md`
preserves the per-variant draft content for historical reference.

The 8 original branches `address-worklist-commit-loop-{1..8}` each received a
sample-then-revert pair before being deleted, leaving the archives here as the
canonical preservation path.
