# `force` — overwrite an existing artifact instead of aborting

Makes design-skill overwrite an existing `artifact_path` instead of
aborting. Default: `false`; rejected with `persistence=chat`.

- **Used by:** design-skill
- **Full entry:** [concerns/lifecycle.md](../concerns/lifecycle.md#artifact-specific)
