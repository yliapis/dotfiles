# `rollup` — mode for rolling up substantive variant content

Rollup mode for substantive variant content when wrap-up archives
worktrees: `interactive` | `auto` | `off`. Default: `interactive` when
`-i` is set, else `off`.

- **Used by:** wrap-up
- **Full entry:** [concerns/selection.md](../concerns/selection.md#wrap-up)
