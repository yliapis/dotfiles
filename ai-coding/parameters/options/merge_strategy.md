# `merge_strategy` — merge-commit shape

The merge-commit shape merge-commit-push uses: `--no-ff` (default) |
`--ff-only` | `--squash`. `--squash` additionally requires
`{commit_message}`.

- **Used by:** merge-commit-push
- **Full entry:** [concerns/lifecycle.md](../concerns/lifecycle.md#artifact-specific)
