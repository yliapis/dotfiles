# `commit_message` — squash commit message

Commit message for the squash commit; required when `{merge_strategy}` is
`--squash`.

- **Used by:** merge-commit-push
- **Full entry:** [concerns/lifecycle.md](../concerns/lifecycle.md#artifact-specific)
