# `trajectory_path` — session trajectory log destination

Path for the session trajectory log wrap-up writes when
`{save_trajectory}` allows it. Default:
`.ai-coding-artifacts/trajectories/<utc-iso-timestamp>.jsonl`.

- **Used by:** wrap-up
- **Full entry:** [concerns/io.md](../concerns/io.md#artifact-specific)
