# `deliverable_shape` — output document shape

The shape of designer-controller's output document: `union` (default —
per-trait deliverables under one umbrella report) | `schema-doc` | `adr`
| `prd` | `spec` | inline template string.

- **Used by:** designer-controller
- **Full entry:** [concerns/composition.md](../concerns/composition.md#artifact-specific)
