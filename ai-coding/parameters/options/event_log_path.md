# `event_log_path` — where wrap-up persists its JSONL event log

Path where wrap-up's JSONL event log is persisted; events are always also
emitted inline. Default:
`.ai-coding-artifacts/wrap-up/<utc-iso-timestamp>.jsonl`.

- **Used by:** wrap-up
- **Full entry:** [concerns/io.md](../concerns/io.md#artifact-specific)
