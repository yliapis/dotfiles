# `aggregator` — producer of a merged artifact from candidates

Prompt or shell command that produces one merged artifact from the
surviving candidates. Required when `synthesize` or `hybrid` is in
`{selection_modes}`.

- **Used by:** agent-swarm
- **Full entry:** [concerns/selection.md](../concerns/selection.md#agent-swarm)
