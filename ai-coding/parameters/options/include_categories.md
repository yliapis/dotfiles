# `include_categories` — only wrap up matching worktree categories

Restricts wrap-up to worktrees whose name begins with one of these
category prefixes. Default: every non-base, non-calling worktree.

- **Used by:** wrap-up
- **Full entry:** [concerns/isolation.md](../concerns/isolation.md#artifact-specific)
