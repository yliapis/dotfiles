# `precedence` — trait-conflict resolution rule

How designer-controller resolves conflicts between traits: `none`
(default) | ordered trait list | explicit rule string. Only meaningful in
`mode=propose` with two or more traits; rejected otherwise.

- **Used by:** designer-controller
- **Full entry:** [concerns/composition.md](../concerns/composition.md#artifact-specific)
