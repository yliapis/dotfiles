# `archive_root` — root directory for wrap-up's category archives

Root directory under which wrap-up archives worktree content by category.
Default: `ai-coding/samples/`.

- **Used by:** wrap-up
- **Full entry:** [concerns/io.md](../concerns/io.md#artifact-specific)
