# `trait_name` — identifier for the trait skill being authored

Kebab-case identifier (`^[a-z][a-z0-9-]*$`) for the trait skill
design-skill is authoring; becomes the directory name
(`<trait_name>-design/`) and the trait-map row. Required.

- **Used by:** design-skill
- **Full entry:** [concerns/intent.md](../concerns/intent.md#artifact-specific)
