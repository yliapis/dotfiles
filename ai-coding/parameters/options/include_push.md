# `include_push` — push after a successful merge

Whether merge-commit-push pushes the target branch to `{remote}` after a
successful merge. Default: `true`.

- **Used by:** merge-commit-push
- **Full entry:** [concerns/lifecycle.md](../concerns/lifecycle.md#artifact-specific)
