# `todo_source` — where the thread's TODO list is read from

Where soft-shutdown reads the thread's TODO/task list from: `agent`
(default — the calling agent's in-memory list) | `path:<file>` | `none`.

- **Used by:** soft-shutdown
- **Full entry:** [concerns/interaction.md](../concerns/interaction.md#artifact-specific)
