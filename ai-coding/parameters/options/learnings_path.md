# `learnings_path` — session learnings note destination

Path for the session learnings note wrap-up writes when
`{save_trajectory}` allows it. Default:
`.ai-coding-artifacts/learnings/<utc-iso-timestamp>.md`.

- **Used by:** wrap-up
- **Full entry:** [concerns/io.md](../concerns/io.md#artifact-specific)
