# `include_sections` — subset of snapshot sections to emit

Ordered subset of the documented section slugs for the session snapshot;
omitted sections must not appear in the output. Default: all sections in
documented order.

- **Used by:** save-session-state
- **Full entry:** [concerns/io.md](../concerns/io.md#artifact-specific)
