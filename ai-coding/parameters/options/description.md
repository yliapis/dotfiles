# `description` — one-paragraph spec for the trait being modeled

One-paragraph specification of the trait the new design skill models.
Required in `mode=propose`; collected during round 0 in `mode=walkthrough`
when not pinned at invocation.

- **Used by:** design-skill
- **Full entry:** [concerns/intent.md](../concerns/intent.md#artifact-specific)
