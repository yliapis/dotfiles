# `exclude_categories` — skip matching worktree categories

Makes wrap-up skip worktrees whose name begins with one of these category
prefixes. Default: empty.

- **Used by:** wrap-up
- **Full entry:** [concerns/isolation.md](../concerns/isolation.md#artifact-specific)
