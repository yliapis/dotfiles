# `max_items` — hard cap on total worklist items addressed

Hard cap on total items addressed across all agents in one
address-worklist-commit-loop run; items beyond the cap are reported as
`deferred (cap-hit)`. Default: unlimited.

- **Used by:** address-worklist-commit-loop
- **Full entry:** [concerns/constraints.md](../concerns/constraints.md#artifact-specific)
