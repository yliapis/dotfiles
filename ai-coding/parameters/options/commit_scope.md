# `commit_scope` — Conventional Commits scope for per-item commits

Conventional Commits scope used in per-item commit subjects. Default:
derived per item (Where: subfield → nearest heading slug → MCP module →
source basename).

- **Used by:** address-worklist-commit-loop
- **Full entry:** [concerns/lifecycle.md](../concerns/lifecycle.md#artifact-specific)
