# `allow_unpushed` — tolerate unpushed commits at shutdown

Whether soft-shutdown tolerates unpushed commits on the current branch
without blocking shutdown. Default: `false`.

- **Used by:** soft-shutdown
- **Full entry:** [concerns/lifecycle.md](../concerns/lifecycle.md#artifact-specific)
