# `worklist_writeback` — how completed items are marked done

How completed items are marked done in the worklist source: `auto`
(default) | `always` | `never`.

- **Used by:** address-worklist-commit-loop
- **Full entry:** [concerns/lifecycle.md](../concerns/lifecycle.md#artifact-specific)
