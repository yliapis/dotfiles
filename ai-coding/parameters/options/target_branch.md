# `target_branch` — branch merge-commit-push merges into

Branch merged into; must exist locally. Default: `main`.

- **Used by:** merge-commit-push
- **Full entry:** [concerns/lifecycle.md](../concerns/lifecycle.md#artifact-specific)
